import React from 'react';
import './header.css';

function Header() {
    return (
        <header>
            <h1>Vérification du code</h1>
        </header>
    );
}

export default Header;
